/* 
Declare uma variável chamada "sum" e atribua a ela uma função chamada "calculaSum". A função deve receber dois parâmetros e retornar a soma desses parâmetros.

const sum = (Number1, Number2) => Number1 + Number2;

console.log(sum(2, 10));

*/

/* 
Análise as funções abaixo (`myFunction`, `myFunction2` e myFunction3, e ajuste o posicionamento das variáveis e funções internas, para que os códigos dentro de `console.log` que estão retornando `undefined` retornem o valor correto da variável ou função chamada. )


function myFunction() {
  var number1 = 10;
  var number2 = 20;
  console.log("Na função 'myFunction', o primeiro número é ", number1);
  console.log("Na função 'myFunction', o segundo número é ", number2);
  return number1 + number2;
}

myFunction();

}

*/

/*
- myFunction2 

function myfunction2() {
  var number1 = 10;
  var number2 = 20;
  var sum = function sum() {
    return number1 + number2;
  };
  console.log(" A soma de 10 e 20 é igual a", sum ? sum() : undefined);
  return sum();
}

myfunction2();

 */

/* 
 - myFunction3

 function myFunction() {
  var number1 = 40;
  var number2 = 50;
  function sum() {
    return number1 + number2;
  }
  console.log(" A soma de 40 e 50 é igual a", sum());
  return sum();
}
myFunction();

*/
